export const formatCalories = (value: number) => `${Math.round(value)} kcal`;
export const formatMacro = (value: number) => `${Math.round(value)}g`;
export const formatPercent = (value: number) => `${Math.round(value * 100)}%`;
